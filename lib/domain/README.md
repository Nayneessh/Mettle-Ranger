Timer engine, chapter stamping, load calculation and storage policy.

Pure Dart. No Flutter imports, no platform channels, no database types —
the round timer and the storage guard have to be testable without a device.
